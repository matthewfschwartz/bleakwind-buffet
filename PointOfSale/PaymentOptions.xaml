<UserControl x:Class="PointOfSale.PaymentOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border>
            <Button Click="OnCashSelect" Name="cashButton" FontSize="30">Cash</Button>
        </Border>
        <Border Grid.Column="1">
            <Button Click="OnCreditSelect" Name="creditDebitButton" FontSize="30">Credit/Debit</Button>
        </Border>
        <Border Grid.Column="2">
            <Button Click="OnReturnSelect" Name="returnButton" FontSize="30">Return</Button>
        </Border>
    </Grid>
</UserControl>
